export interface ChildProps {
	children: React.ReactNode
}
export interface IProject {
	slug: string
	name: string
	location: string
	volume: number
	direction: string
	image: string
	status: string
}
